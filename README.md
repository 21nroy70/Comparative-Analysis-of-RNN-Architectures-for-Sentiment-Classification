Before running any of the .py files, make sure to run on the terminal (once you're in the directory ofc): pip install -r requirements.txt

Include:

- Setup instructions (Python version, dependencies)
- How to run training and evaluation scripts
- Expected runtime and output files





